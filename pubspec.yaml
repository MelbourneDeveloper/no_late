name: no_late
description: A custom lint package that enforces proper usage of the 'late' keyword for lazy initialization only
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^6.0.0
  analyzer_plugin: ^0.11.0
  custom_lint_builder: ^0.6.10

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
  custom_lint: ^0.6.10